# Remplacer par l'URL de connexion Neon ou Supabase (voir §6 du cahier des charges)
DATABASE_URL="postgresql://user:password@localhost:5432/gestion_equipe?schema=public"

# Auth.js — générer avec `npx auth secret` ou `openssl rand -base64 32`
AUTH_SECRET="changeme-generate-a-real-secret"
